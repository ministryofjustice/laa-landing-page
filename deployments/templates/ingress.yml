apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: laa-landing-page-ingress
  annotations:
    external-dns.alpha.kubernetes.io/set-identifier: laa-landing-page-ingress-${NAMESPACE}-green
    external-dns.alpha.kubernetes.io/aws-weight: "100"
    nginx.ingress.kubernetes.io/affinity: "cookie"
    nginx.ingress.kubernetes.io/session-cookie-name: "sticky"
    nginx.ingress.kubernetes.io/session-cookie-hash: sha1
    nginx.ingress.kubernetes.io/session-cookie-expires: "172800"
    nginx.ingress.kubernetes.io/session-cookie-max-age: "172800"
    nginx.ingress.kubernetes.io/enable-modsecurity: "true"
    nginx.ingress.kubernetes.io/modsecurity-snippet: |
      SecRuleEngine On
      SecAuditEngine On
      SecDefaultAction "phase:2,pass,log,tag:github_team=laa-portal-stabilisation-tech"
      SecDebugLogLevel 1
spec:
  ingressClassName: ${INGRESS_CLASS_NAME}
  tls:
    - hosts:
      - ${BASE_URL_HOMEPAGE}
      secretName: "laa-landing-page-homepage-secret"
    - hosts:
      - ${BASE_URL_MANAGE_USER}
      secretName: "manage-srv-secret"
  rules:
    - host: ${BASE_URL_HOMEPAGE}
      http:
        paths:
# Need to work out what is happening with it - we won't have a route to / if we put this in?? Is the tech
# Services thing going live today to solve this? hasn't been tested etc
# What do we set BASE_URL in secrets manager as??
          - path: /home
            pathType: ImplementationSpecific
            backend:
              service:
                name: laa-landing-page
                port:
                  number: 8080
    - host: ${BASE_URL_MANAGE_USER}
      http:
        paths:
# Need to work out what is happening with it - we won't have a route to / if we put this in?? Is the tech
# Services thing going live today to solve this? hasn't been tested etc
# What do we set BASE_URL in secrets manager as??
          - path: /admin/users
            pathType: ImplementationSpecific
            backend:
              service:
                name: laa-landing-page
                port:
                  number: 8080
