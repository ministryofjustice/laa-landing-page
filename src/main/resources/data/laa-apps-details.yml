laa-apps:
  applications:
    - name: ${app.civil.apply.name}
      oidGroupName: CCMS_Apply
      title: Apply for civil legal aid
      description: >-
        For special children act, public law family, domestic abuse and section 8 proceedings only
      url: ${app.civil.apply.url}
      laaApplicationDetails: ${app.civil.apply.details}
      ordinal: 2
    - name: ${app.crime.apply.name}
      oidGroupName: CRIMEAPPLY
      title: Apply for criminal legal aid
      description: Make an application to represent clients charged with a criminal offence
      url: ${app.crime.apply.url}
      laaApplicationDetails: ${app.crime.apply.details}
      ordinal: 0
    - name: ${app.pui.name}
      oidGroupName: PUI
      title: Client and Cost Management System
      description: For all civil legal aid proceedings, managing applications, billing and payments
      descriptionIfAppAssigned:
        appAssigned: ${app.civil.apply.name}
        description: >-
          For all remaining civil legal aid proceedings, managing applications, billing and payments
      url: /home/pui-interstitial
      laaApplicationDetails: ${app.pui.details}
      ordinal: 3
    - name: ${app.submit.crime.form.name}
      oidGroupName: NEWCRM7
      title: Submit a crime form
      description: >-
        Apply for prior authority to incur disbursements, extension of upper limit, or to claim a non-standard magistrates' court payment
      url: ${app.submit.crime.form.url}
      laaApplicationDetails: ${app.submit.crime.form.details}
      ordinal: 1
    - name: ${app.ccms.account.link.name}
      title: CCMS case transfer requests
      description: >-
        Make and manage requests to transfer CCMS cases from legacy accounts to the new accounts
      url: ${app.ccms.account.link.url}
      laaApplicationDetails: ${app.ccms.account.link.details}
      ordinal: 4
    - name: ${app.submit.a.bulk.claim.name}
      title: Submit A Bulk Claim
      description: >-
        Submit A Bulk Claim
      url: ${app.submit.a.bulk.claim.url}
      laaApplicationDetails: ${app.submit.a.bulk.claim.details}
      ordinal: 5
    - name: ${app.amend.a.claim.name}
      title: Amend a Claim
      description: >-
        Amend a Claim
      url: ${app.amend.a.claim.url}
      laaApplicationDetails: ${app.amend.a.claim.details}
      ordinal: 6
    - name: ${app.civil.decide.name}
      title: Civil Decide
      description: >-
        Civil Decide
      url: ${app.civil.decide.url}
      laaApplicationDetails: ${app.civil.decide.details}
      ordinal: 7
    - name: ${app.civil.claim.for.payment.name}
      title: Civil Claim for Payment
      description: >-
        Civil Claim for Payment
      url: ${app.civil.claim.for.payment.url}
      laaApplicationDetails: ${app.civil.claim.for.payment.details}
      ordinal: 8
    - name: ${app.means.and.match.ui1.name}
      title: LAA Means And Match Spike UI 1
      description: >-
        LAA Means And Match Spike UI 1
      url: ${app.means.and.match.ui1.url}
      laaApplicationDetails: ${app.means.and.match.ui1.details}
      ordinal: 9
    - name: ${app.means.and.match.ui2.name}
      title: LAA Means And Match Spike UI 2
      description: >-
        LAA Means And Match Spike UI 2
      url: ${app.means.and.match.ui2.url}
      laaApplicationDetails: ${app.means.and.match.ui2.details}
      ordinal: 10
